# author:baiji
# use read function to read the script file
# use shuffle and makebatch function to get the training batch
# use load function to load the script data
# use operations to handle the loaded

import random

class lable_loader():
    def load_instances(file_path):
        with open(inst_file, encoding='utf-8') as f:

    def build_vocab():

def load_dataset(feature_script, label_scrpit):
    with open(path, 'r') as file:
        


def save():
    return

def load():